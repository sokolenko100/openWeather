import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        margin: 'auto',
    },
    title: {
        fontWeight: '600',
        fontSize: 24,
        marginBottom: 20,
    },
    text: {
        fontWeight: '400',
        fontSize: 20,
        marginBottom: 20,
    },
});
